import { RemoveFormFeedPipe } from './remove-form-feed.pipe';

describe('RemoveFormFeedPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveFormFeedPipe();
    expect(pipe).toBeTruthy();
  });
});
